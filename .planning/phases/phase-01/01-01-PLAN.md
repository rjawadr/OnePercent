---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/db/client.ts, src/store/habitStore.ts, src/services/NotificationService.ts, src/db/schema.ts]
autonomous: true
requirements: [SYST-01, SYST-02, SYST-03]
must_haves:
  truths:
    - "SQLite database is initialized and accessible via JSI"
    - "Notifee is configured for local notifications"
    - "Zustand store persists data to SQLite"
  artifacts:
    - path: "src/db/client.ts"
      provides: "SQLite database instance"
    - path: "src/services/NotificationService.ts"
      provides: "Notifee wrapper"
---

<objective>
Setup core infrastructure: high-performance SQLite storage (Nitro-SQLite), local notifications (Notifee), and state management (Zustand).

Purpose: Provides the "local-first" foundation for the entire app.
Output: Database client, notification service, and persistent store.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install peer dependencies</name>
  <action>Install react-native-nitro-sqlite, @notifee/react-native, and zustand. Note: react-native-nitro-sqlite is chosen as the modern JSI-based successor to quick-sqlite for 2026/RN 0.84.</action>
  <verify>package.json contains dependencies</verify>
  <done>Dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Nitro-SQLite Client</name>
  <files>src/db/client.ts, src/db/schema.ts</files>
  <action>Create a database client that opens 'OnePercent.db'. Define the initial schema for 'habits' and 'logs' tables. Implement a simple migration runner.</action>
  <verify>Database successfully opens on physical device</verify>
  <done>SQLite client operational</done>
</task>

<task type="auto">
  <name>Task 3: Setup Zustand Habit Store</name>
  <files>src/store/habitStore.ts</files>
  <read_first>src/db/client.ts</read_first>
  <action>Create a Zustand store for habits. Integrate with Nitro-SQLite for persistence (using a custom storage engine for zustand-persist). Implement basic select/insert actions.</action>
  <verify>Store state survives app reload</verify>
  <done>Persistent store operational</done>
</task>

<task type="auto">
  <name>Task 4: Setup Notifee Service</name>
  <files>src/services/NotificationService.ts</files>
  <action>Initialize Notifee. Create a wrapper service for displaying local notifications, scheduling daily reminders, and handling interaction events.</action>
  <verify>Display a test notification</verify>
  <done>Notification service operational</done>
</task>

</tasks>

<verification>
- [ ] npm run build/android succeeds
- [ ] SQLite database file exists on device
- [ ] Test notification appears on device
</verification>

<success_criteria>
- All infrastructure components initialized
- Local-first persistence works (survives reload)
- Notifications work offline
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
