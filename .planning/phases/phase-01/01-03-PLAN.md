---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/engine/onePercentEngine.ts, src/models/Habit.ts, src/models/DailyLog.ts, src/store/habitStore.ts]
autonomous: true
requirements: [HBIT-02]
must_haves:
  truths:
    - "1% compounding math is accurate (daily/weekly/monthly)"
    - "Regression logic (0.95x) applies after 2+ missed days"
    - "Calculation handles quantities and checkmarks correctly"
  artifacts:
    - path: "src/engine/onePercentEngine.ts"
      provides: "Compounding math logic"
    - path: "src/models/Habit.ts"
      provides: "Habit interface/TS types"
---

<objective>
Implement the 1% Growth Engine: the core mathematical logic for habit compounding, regression, and status calculation.

Purpose: The project's unique selling point and core logic layer.
Output: Compounding math module, Habit/Log types, and store logic integration.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@prd.md (Engine Logic section)
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Habit & DailyLog Models</name>
  <files>src/models/Habit.ts, src/models/DailyLog.ts</files>
  <action>Define Habit interface: id, name, icon, color, baseline_value, current_value, target_value, goal_value, unit, frequency (daily/weekly/monthly), streak, etc. Define DailyLog interface: habit_id, date, value_achieved, target_at_time.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Models defined</done>
</task>

<task type="auto">
  <name>Task 2: 1% Compounding Math Module</name>
  <files>src/engine/onePercentEngine.ts</files>
  <action>Implement calculateNextTarget(baseline, frequency, daysElapsed). Ensure it supports 1% daily (37.8x yearly), 1% weekly (1.6x yearly), and 1% monthly (1.12x yearly).</action>
  <verify>Unit tests (using jest/simple assertions) verify 1.01^365 ~= 37.8</verify>
  <done>Compounding engine operational</done>
</task>

<task type="auto">
  <name>Task 3: Missed Days & Regression Logic</name>
  <files>src/engine/onePercentEngine.ts</files>
  <action>Implement calculateAdjustedTarget(lastTarget, missedDays). Rule 1: Missed 1 day -> Next target = lastTarget. Rule 2: Missed 2+ days -> Next target = lastTarget * 0.95 (graceful regression).</action>
  <verify>Logic correctly reduces target after 2-day gap</verify>
  <done>Regression logic operational</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Logic into Zustand Store</name>
  <files>src/store/habitStore.ts</files>
  <read_first>src/engine/onePercentEngine.ts, src/store/habitStore.ts</read_first>
  <action>Add actions to the store: createHabit, logProgress, calculateDailyTargets. Ensure store computes the target for 'Today' whenever called, using the engine logic.</action>
  <verify>Store correctly tracks streaks and next targets</verify>
  <done>Store-Engine integration complete</done>
</task>

</tasks>

<verification>
- [ ] Streak increases correctly on log
- [ ] Target value increases by ~1% on daily habit completion
- [ ] Target value remains same after 1 missed day
</verification>

<success_criteria>
- Accurate 1% math.
- Handling of "baseline" vs "current target."
- Streak persistence logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
