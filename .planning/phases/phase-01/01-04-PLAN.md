---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified: [src/screens/TodayScreen.tsx, src/components/HabitCard.tsx, src/components/CreateHabitModal.tsx, src/App.tsx, src/navigation/RootNavigator.tsx]
autonomous: true
requirements: [HBIT-01, HBIT-03, HBIT-05]
must_haves:
  truths:
    - "Today screen displays all active habits"
    - "User can create a new habit with baseline/unit/frequency"
    - "Logging progress immediately updates the UI and store"
  artifacts:
    - path: "src/screens/TodayScreen.tsx"
      provides: "Main home screen"
    - path: "src/components/HabitCard.tsx"
      provides: "Habit list item"
---

<objective>
Build the "Today" screen UI, Habit cards, and the "Create Habit" flow.

Purpose: Completes the MVP loop for habit tracking.
Output: Working Today screen, Habit card component, and Create Modal.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@appflow.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Premium HabitCard Component</name>
  <files>src/components/HabitCard.tsx</files>
  <read_first>src/theme/design_tokens.ts</read_first>
  <action>Create a card with progress bar (percentage of daily target), icon, and quick-log buttons (+/- increment for units, check/uncheck for checkboxes). Use Brand Teal accents. Follow 'Anxiety-Safe' design (no aggressive red bars).</action>
  <verify>Card renders with correct dynamic progress</verify>
  <done>HabitCard component ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement Create Habit Modal</name>
  <files>src/components/CreateHabitModal.tsx</files>
  <action>Build a bottom-sheet styled modal for Add/Edit. Fields: Name, Category, Baseline Value (number), Unit (Text), Frequency (Daily/Weekly/Monthly). Save to habitStore.</action>
  <verify>Submitting the form adds habit to today's list</verify>
  <done>Create flow operational</done>
</task>

<task type="auto">
  <name>Task 3: Implement Today Home Screen</name>
  <files>src/screens/TodayScreen.tsx</files>
  <read_first>src/store/habitStore.ts, src/components/HabitCard.tsx</read_first>
  <action>Render 'Today's Focus' header with summary progress circle. List all habits using FlatList. Handle empty state with 'First habit' CTA. Implement pull-to-refresh if needed sync. Link '+' tab to Create Modal.</action>
  <verify>List updates in real-time when new habits added</verify>
  <done>Home screen fully built</done>
</task>

<task type="auto">
  <name>Task 4: Main Layout Integration</name>
  <files>src/App.tsx, src/navigation/RootNavigator.tsx</files>
  <action>Update App.tsx to provide Zustand persistence provider (if needed) and Navigation Container. Connect RootNavigator to actual TodayScreen instead of placeholders.</action>
  <verify>Physical device boots into TodayScreen</verify>
  <done>App end-to-end flow operational</done>
</task>

</tasks>

<verification>
- [ ] Add a daily habit: 'Pushups' (Baseline: 10, Unit: Reps, Frequency: Daily)
- [ ] Log 10 pushups -> Progress shows 100%
- [ ] Reboot app -> State persists correctly
</verification>

<success_criteria>
- Today screen displays correctly.
- Add Habit flow works end-to-end.
- State persists across reloads.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
