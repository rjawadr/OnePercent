---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/theme/design_tokens.ts, src/navigation/RootNavigator.tsx, src/components/ui/Layout.tsx, src/theme/fonts.ts, src/components/ui/Button.tsx]
autonomous: true
requirements: [DSGN-01, DSGN-02]
must_haves:
  truths:
    - "Brand Teal (#3EC9A7) is the primary color theme"
    - "Bottom tab navigation is active"
    - "App reflects 'Calm/Premium' aesthetic with modern fonts (Outfit/Inter)"
  artifacts:
    - path: "src/theme/design_tokens.ts"
      provides: "Colors, Spacing, Shadows"
    - path: "src/navigation/RootNavigator.tsx"
      provides: "Main tab navigation"
---

<objective>
Setup the design system (Design Tokens, Typography, Layout) and initial app navigation (React Navigation Tabs).

Purpose: Establishes the visual identity and routing structure.
Output: Design tokens, base UI components, and navigation container.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontendguideline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Navigation dependencies</name>
  <action>Install @react-navigation/native, @react-navigation/bottom-tabs, react-native-screens, and react-native-safe-area-context.</action>
  <verify>package.json reflects navigation libs</verify>
  <done>Navigation libs installed</done>
</task>

<task type="auto">
  <name>Task 2: Implement Design Tokens</name>
  <files>src/theme/design_tokens.ts, src/theme/fonts.ts</files>
  <read_first>frontendguideline.md</read_first>
  <action>Define Brand Teal (#3EC9A7), Deep Navy (#1C2B4A), and Warm White (#F5F6F8). Configure spacing, corner radius (8px/12px), and shadows. Link Outfit/Inter fonts (mocked if assets missing, then confirm with user or using Google Fonts via RN patterns).</action>
  <verify>Types exported and usable in UI components</verify>
  <done>Design system tokens established</done>
</task>

<task type="auto">
  <name>Task 3: Create Bottom Tab Navigator</name>
  <files>src/navigation/RootNavigator.tsx</files>
  <action>Setup navigation with Today, Stats, Add (+), Friends, and Settings tabs as per App Flow. Create placeholder screens for now. Customize tab bar style (glassmorphism/blur if possible or clean solid white/navy per guidelines).</action>
  <verify>App renders bottom tab bar</verify>
  <done>Navigation structure active</done>
</task>

<task type="auto">
  <name>Task 4: Build Core UI components</name>
  <files>src/components/ui/Layout.tsx, src/components/ui/Button.tsx</files>
  <action>Create a Layout component with Safe Area. Create a premium Button component supporting Primary, Secondary, and Ghost styles using Brand Teal.</action>
  <verify>Screens use Layout and Button correctly</verify>
  <done>Base UI components ready</done>
</task>

</tasks>

<verification>
- [ ] Tabs are switchable on physical device
- [ ] Safe Area padding is correct for various device notches
- [ ] Button reflecting the Brand Teal theme
</verification>

<success_criteria>
- Design tokens implemented in theme.
- Navigation structure matching app flow.
- "Today" tab is default.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
